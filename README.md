# Light Trail Generator

1. Load a video of robot trajectories (works best top-down)
2. Click on each robot in the first frame to initialize tracking
3. Adjust detection/style parameters
4. Run tracking + render glowing light trails (by default, rendered onto last frame)

# Requirements:
    `pip install opencv-python numpy pillow`